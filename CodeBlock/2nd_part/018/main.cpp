#include <iostream>
#include <fstream>

using namespace std;

int main()
{
    int i, n, m, a[1000], b[1000];
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    for (i = 1; i <= m; i++)
        cin >> a[i];
    for (i = 1; i <= m; i++)
        cin >> b[i];
    return 0;
}
